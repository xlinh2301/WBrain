# api-first-platform

Build the persistent WBrain API before generating a public SDK.

The API is the source of truth for meter, image metadata, reading, review,
model-version, and audit contracts. SDK generation follows after these
contracts are tested and versioned.
